apiVersion: v1
kind: Pod
metadata:
  name: hello-angular
  labels:
    name: hello-angular
spec:
  containers:
  - name: Hello-docker
    image: raviteja01/welcome-app:hello
    resources:
      limits:
        memory: "128Mi"
        cpu: "500m"
    ports:
      - containerPort: 80
